apiVersion: v1
kind: Service
metadata:
  name: atelier-app
  labels:
    app: atelier-app
spec:
  selector:
    app: atelier-app
  ports:
    - name: http
      port: 80
      targetPort: 3000
  type: NodePort

